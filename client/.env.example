# Vercel Environment Variables Setup
# Copy this file to .env for local development
# DO NOT commit .env to git (it's in .gitignore)

# Backend API URL
# For local development, use: http://localhost:5000/api
# For production, set this in Vercel dashboard: https://your-backend-api.com/api
VITE_API_URL=http://localhost:5000/api

# Note: In Vercel, set this in Dashboard → Settings → Environment Variables
# The variable name must start with VITE_ to be accessible in client-side code
